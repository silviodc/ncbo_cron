# Local gemfile
# This gets included by a back-door in bundler so it doesn't affect
# the Gemfile.lock (which gets committed to avoid different dependency versions)

source 'https://rubygems.org'

gem 'sparql-client', :git => 'https://github.com/ncbo/sparql-client.git'
gem 'goo', :path => '~/dev/ncbo/goo'
gem 'ontologies_linked_data', :path => '~/dev/ncbo/ontologies_linked_data'
